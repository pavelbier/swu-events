{
  "name": "events-map-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npx rimraf dist && vite build && git -C dist init && git -C dist checkout -b page && git -C dist add -A && git -C dist commit -m \"deploy\" && git -C dist push git@github.com:pavelbier/swu-events.git page --force"
  },
  "dependencies": {
    "@vue-leaflet/vue-leaflet": "^0.10.1",
    "dayjs": "^1.11.19",
    "leaflet": "^1.9.4",
    "pinia": "^3.0.4",
    "vue": "^3.5.24"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.2.4"
  }
}
